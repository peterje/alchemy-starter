pre-commit:
  jobs:
    - name: format
      run: bun run format:check
    - name: lint
      run: bun run lint

commit-msg:
  jobs:
    - name: conventional-commit
      run: bun commitlint --edit {1}
